version: '3'

services:
  test-db-mysql:
    image: mysql:8.0.33-debian
    restart: always
    ports:
      - 3307:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: meupetminhavidatestdb
